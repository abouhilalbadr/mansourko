/**
 * Mansourko Hub — intentionally empty. The hero art is the content; whether he
 * is on air is answered by the HUD above it, on every page.
 */
export default function HubPage() {
  return <div className="h-full w-full" />;
}
